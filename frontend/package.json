{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "wasm:build": "RUSTFLAGS='-C opt-level=3 -C codegen-units=1 -C target-feature=+simd128' wasm-pack build src/wasm/combination --target web --release --out-dir pkg",
    "build": "npm run wasm:build && vite build"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.0",
    "prettier": "^3.6.2",
    "prettier-plugin-svelte": "^3.4.0",
    "svelte": "^5.39.4",
    "svelte-preprocess": "^6.0.3",
    "vite": "^7.1.7",
    "vite-plugin-compression": "^0.5.1"
  },
  "dependencies": {
    "lucide-svelte": "^0.545.0",
    "lz-string": "^1.5.0",
    "marked": "^17.0.1",
    "svelte-portal": "^2.2.1"
  }
}
